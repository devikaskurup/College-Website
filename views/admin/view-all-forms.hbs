<link rel="stylesheet" href="/stylesheets/view-all-forms.css" />

<div class="main-container">
  <div class="title">
    <h1>Forms</h1>
    <a href="/admin/add-new-form" class="btn">Add New Form</a>
  </div>
  <div class="sub-contents">
    <table id="formsTable">
      <thead>
        <tr>
          <th>Title</th>
          <th>Date</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        {{#each formsList}}
          <tr>
            <td>{{title}}</td>
            <td>{{dateAdded}}</td>
            <td><a
                href="/admin/edit-form-page/{{_id}}"
                class="edit-btn"
              >Edit</a>&nbsp;<a
                href="/admin/delete-form/{{_id}}"
                class="delete-btn"
                onclick="return confirm('Are you sure you want to delete this Form ?');"
              >Delete</a></td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>
<script>
  $( function () { $('#formsTable').DataTable(); } );
</script>